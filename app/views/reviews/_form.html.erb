<div class="container" id="form_review">
  <%= simple_form_for [@restaurant, @review] do |f| %>
    <%= f.input :content %>
    <%= f.input :rating, collection: Review::RATINGS, prompt: "Select a Rating" %>
    <%= f.submit "Add a New Review", class: "btn btn-primary" %>
  <% end %>
</div>
